<%= form_for([@post, @post.comments.build], remote: true) do |f| %>
  <p>
  	<%= f.hidden_field :user_id, :value => current_user.id, :spellcheck => "true" %>
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </p>
  <p>
    <%= f.submit "Add Comment" %>
  </p>
<% end %>